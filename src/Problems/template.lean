import Geo.Geo.Core

namespace Geo

-- e.g., open Geo.Triangle

-- def <IMO_<YEAR>_<PX> : Prop := ∀ (A, B, .. : Point), <pred>(subset of points) → rest of problem...
-- in the above, can include statements like the following:
-- let γ := circumcircle ⟨points...⟩; → <pred>(...)

end Geo
